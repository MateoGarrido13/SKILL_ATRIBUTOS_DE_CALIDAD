# Caso 06 — Hospital, requerimiento que mezcla 2 atributos — input para `generar-escenarios-calidad`

Caso adversarial tomado de la rama `mateo` (`docs/test/PRUEBA_UNO_HOSPITAL/`). Objetivo del caso: verificar que la skill **separa** un requerimiento que mezcla dos atributos de calidad distintos en dos escenarios independientes, en vez de forzarlos en uno solo o quedarse solo con el primero que aparece en el texto.

## Input (enunciado informal, tal cual)

"El sistema de gestión de turnos del hospital tiene que ser seguro para que no entren intrusos, y además tiene que andar bien cuando hay mucha gente sacando turnos al mismo tiempo porque si se cuelga nos matan."
